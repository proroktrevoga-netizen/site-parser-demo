# Пример переменных окружения для парсера
# Скопируй файл в .env и заполни нужные значения

# Прокси (опционально)
# HTTP_PROXY=http://user:pass@host:port
# HTTPS_PROXY=http://user:pass@host:port

# Задержка между запросами в секундах (переопределяет config.py)
# REQUEST_DELAY=1.0
